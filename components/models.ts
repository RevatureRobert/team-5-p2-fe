export interface iThread {
    id: number;
    title: string;
    author: string;
    description: string;
}

export interface iUser {
    id: number;
    username: string;
    password: string;
    email: string;
}